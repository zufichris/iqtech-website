export interface IService {
    title: string,
    slug: string,
    description: string,
    icon: string,
    image: string,
    technologies: string[],
    features: string[],
    benefits: string[],
    process: string[]
}